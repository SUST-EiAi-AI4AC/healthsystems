<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.nwpu.healthsystem.backend.mapper.scale.AiChatMapper">

    <!-- ==================== ResultMap ==================== -->
    <resultMap id="AiChatSessionMap" type="com.nwpu.healthsystem.backend.entity.scale.AiChatSession">
        <id     column="id"         property="id"/>
        <result column="user_id"    property="userId"/>
        <result column="title"      property="title"/>
        <result column="created_at" property="createdAt"/>
        <result column="updated_at" property="updatedAt"/>
    </resultMap>

    <resultMap id="AiChatMessageMap" type="com.nwpu.healthsystem.backend.entity.scale.AiChatMessage">
        <id     column="id"         property="id"/>
        <result column="session_id" property="sessionId"/>
        <result column="user_id"    property="userId"/>
        <result column="role"       property="role"/>
        <result column="content"    property="content"/>
        <result column="msg_type"   property="msgType"/>
        <result column="created_at" property="createdAt"/>
    </resultMap>

    <!-- ==================== Session: 新增 ==================== -->
    <insert id="insertSession" useGeneratedKeys="true" keyProperty="id">
        INSERT INTO ai_chat_session (user_id, title)
        VALUES (#{userId}, #{title})
    </insert>

    <!-- ==================== Session: 更新标题 ==================== -->
    <update id="updateSessionTitle">
        UPDATE ai_chat_session
        SET title = #{title}
        WHERE id = #{id}
    </update>

    <!-- ==================== Session: 刷新 updated_at ==================== -->
    <update id="touchSession">
        UPDATE ai_chat_session
        SET updated_at = NOW()
        WHERE id = #{id}
    </update>

    <!-- ==================== Session: 按用户查询列表（降序） ==================== -->
    <select id="listSessionsByUser" resultMap="AiChatSessionMap">
        SELECT id, user_id, title, created_at, updated_at
        FROM ai_chat_session
        WHERE user_id = #{userId}
        ORDER BY updated_at DESC
    </select>

    <!-- ==================== Session: 根据ID查单条 ==================== -->
    <select id="getSessionById" resultMap="AiChatSessionMap">
        SELECT id, user_id, title, created_at, updated_at
        FROM ai_chat_session
        WHERE id = #{id}
    </select>

    <!-- ==================== Session: 删除（外键 CASCADE 同时删消息） ==================== -->
    <delete id="deleteSession">
        DELETE FROM ai_chat_session
        WHERE id = #{id}
    </delete>

    <!-- ==================== Message: 批量插入 ==================== -->
    <insert id="batchInsertMessages" useGeneratedKeys="true" keyProperty="id">
        INSERT INTO ai_chat_message (session_id, user_id, role, content, msg_type)
        VALUES
        <foreach collection="list" item="m" separator=",">
            (#{m.sessionId}, #{m.userId}, #{m.role}, #{m.content}, #{m.msgType})
        </foreach>
    </insert>

    <!-- ==================== Message: 查询某会话全部消息（升序） ==================== -->
    <select id="listMessagesBySession" resultMap="AiChatMessageMap">
        SELECT id, session_id, user_id, role, content, msg_type, created_at
        FROM ai_chat_message
        WHERE session_id = #{sessionId}
        ORDER BY created_at ASC, id ASC
    </select>

    <!-- ==================== Message: 删除某会话全部消息 ==================== -->
    <delete id="deleteMessagesBySession">
        DELETE FROM ai_chat_message
        WHERE session_id = #{sessionId}
    </delete>

</mapper>
